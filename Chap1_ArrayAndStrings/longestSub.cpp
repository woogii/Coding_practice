#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int compare(const void *a, const void *b)
{
	return strcmp( *(char**)a, *(char**)b );
}
int comlen(const void *a, const void *b)
{

}


int main()
{
	char str[] = "abcdaabcdefabcf";
	int len = strlen(buf);
	int maxlen = 0;
	int idx = 0;
	int tmp = 0;
	int i = 0;

	char *ch[len];

	for(i = 0; i < len; i++)



}