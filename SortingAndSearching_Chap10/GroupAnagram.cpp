#include <iostream>
#include <map>
#include <vector>
#include <algorithm>
#include <string>

std::vector<std::string> anagrams(std::vector<std::string> &strs);

int main()
{
    std::vector<std::string> array;

    array.push_back("apple");
    array.push_back("banana");
    array.push_back("carrot");
    array.push_back("ele");
    array.push_back("duck");
    array.push_back("papel");
    array.push_back("tarroc");
    array.push_back("cudk");
    array.push_back("eel");
    array.push_back("lee");

}


std::vector<std::string> anagrams(std::vector<std::string> &strs)
{


}
